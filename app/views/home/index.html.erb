<div class="jumbotron jumbotron-fluid">
  <div class="container">
    <h1 class="display-4">Bienvenue</h1>
    <p class="lead">Dieunde est la plateforme de vente au Sénégal</p>
  </div>

<section class="search-sec">
    <div class="container">
        <%= form_tag(root_path, method: :get) do %>
            <div class="row">
                <div class="col-lg-12">
                    <div class="row">
                        <div class="col-lg-8 col-md-8 col-sm-12 p-0">
                            <%= text_field_tag(:recherche, params[:recherche], class: 'form-control search-slt', placeholder: 'Ex: Samsung Galaxy S21') %>
                        </div>
                        <!-- <div class="col-lg-3 col-md-3 col-sm-12 p-0">
                            <input type="text" class="form-control search-slt" placeholder="Enter Drop City">
                        </div> -->
                        <!-- <div class="col-lg-3 col-md-3 col-sm-12 p-0">
                            <select class="form-control search-slt" id="exampleFormControlSelect1">
                                <option>Select Vehicle</option>
                                <option>Example one</option>
                                <option>Example one</option>
                                <option>Example one</option>
                                <option>Example one</option>
                                <option>Example one</option>
                                <option>Example one</option>
                            </select>
                        </div> -->
                        <div class="col-lg-4 col-md-3 col-sm-12 p-0">
                          <%= submit_tag('Rechercher', class: 'btn btn-danger wrn-btn') %>
                            <!-- <button type="button" class="btn btn-danger wrn-btn">Search</button> -->
                        </div>
                    </div>
                </div>
            </div>
        <% end %>
    </div>
</section>
</div>


<h2 class="text-center">Produits</h2>
<div class="produits container">

  <% @produits.each do |produit| %>
  <div class="card produit">
    <div class="col-md-3">
      <%= (image_tag produit.images.first, class: 'card-img-top') if produit.images.attached? %>
    </div>

  <div class="card-body col-md-8 offset-1">
    <h5 class="card-title"><%= produit.nom %></h5>
    <p class="card-text"><%= produit.description.truncate(100, separator: /\s/) if produit.description?%></p>
    <p class="card-text"><%= produit.prix %> cfa</p>
    <p class="badge bg-secondary"><%=produit.catégorie %></p> <br/>
    <%= link_to "Voir", produit_path(produit), class:"btn btn-primary" %>
  </div>
</div>
  <% end %>
</div>
